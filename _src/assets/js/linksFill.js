const inputGithub = document.querySelector('.input_box__github');

const linkToGithub = document.querySelector('.github__link');

const fillLinkGithub = () => {
    if(inputGithub.value === "") {
        linkToGithub.href = "";
    }else {
        inputGithub.valur = 
        linkToGithub.href
    }
}